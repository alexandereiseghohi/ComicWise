import type { ReactNode } from "react";

/**
 * Auth layout component
 * Handles authentication pages (sign-in, sign-up, password reset, etc.)
 * @param root0
 * @param root0.children
 */
export default function AuthLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
