import type { ReactNode } from "react";

/**
 * Layout for verify request page
 * @param root0
 * @param root0.children
 */
export default function VerifyRequestLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
