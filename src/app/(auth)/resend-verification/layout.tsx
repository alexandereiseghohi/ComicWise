import type { ReactNode } from "react";

/**
 * Layout for resend verification page
 * @param root0
 * @param root0.children
 */
export default function ResendVerificationLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
