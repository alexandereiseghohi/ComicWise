import type { ReactNode } from "react";

/**
 * Layout for sign-out page
 * @param root0
 * @param root0.children
 */
export default function SignOutLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
