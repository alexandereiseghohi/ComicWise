import type { ReactNode } from "react";

/**
 * Layout for comics section (root)
 * @param root0
 * @param root0.children
 */
export default function ComicsLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
