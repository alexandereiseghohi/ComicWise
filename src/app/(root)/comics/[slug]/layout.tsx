import type { ReactNode } from "react";

/**
 * Layout for individual comic pages (root)
 * @param root0
 * @param root0.children
 */
export default function ComicLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
