import type { ReactNode } from "react";

/**
 * Layout for chapter reader pages (root)
 * @param root0
 * @param root0.children
 */
export default function ChapterReaderLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
