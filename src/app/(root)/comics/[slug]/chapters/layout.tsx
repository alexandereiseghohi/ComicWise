import type { ReactNode } from "react";

/**
 * Layout for comic chapters section (root)
 * @param root0
 * @param root0.children
 */
export default function ChaptersLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
