import type { ReactNode } from "react";

/**
 * Layout for bookmarks section (root)
 * @param root0
 * @param root0.children
 */
export default function BookmarksLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
