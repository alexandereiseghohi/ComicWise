import type { ReactNode } from "react";

/**
 * Layout for browse section
 * @param root0
 * @param root0.children
 */
export default function BrowseLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
