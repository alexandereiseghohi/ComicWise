import type { ReactNode } from "react";

/**
 * Layout for terms of service page
 * @param root0
 * @param root0.children
 */
export default function TermsOfServiceLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
