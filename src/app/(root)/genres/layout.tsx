import type { ReactNode } from "react";

/**
 * Layout for genres section
 * @param root0
 * @param root0.children
 */
export default function GenresLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
