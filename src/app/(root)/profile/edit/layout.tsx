import type { ReactNode } from "react";

/**
 * Layout for profile editing pages (root)
 * @param root0
 * @param root0.children
 */
export default function EditProfileLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
