import type { ReactNode } from "react";

/**
 * Layout for viewing other user profiles (root)
 * @param root0
 * @param root0.children
 */
export default function UserProfileLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
