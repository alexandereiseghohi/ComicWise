import type { ReactNode } from "react";

/**
 * Layout for profile settings pages (root)
 * @param root0
 * @param root0.children
 */
export default function SettingsLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
