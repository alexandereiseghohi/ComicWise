import type { ReactNode } from "react";

/**
 * Layout for password change pages (root)
 * @param root0
 * @param root0.children
 */
export default function ChangePasswordLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
