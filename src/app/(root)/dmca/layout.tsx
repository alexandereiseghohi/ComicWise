import type { ReactNode } from "react";

/**
 * Layout for DMCA page
 * @param root0
 * @param root0.children
 */
export default function DMCALayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
