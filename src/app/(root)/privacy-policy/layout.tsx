import type { ReactNode } from "react";

/**
 * Layout for privacy policy page
 * @param root0
 * @param root0.children
 */
export default function PrivacyPolicyLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
