# Active Context

CurrentFocus:

- Stabilize seeders, standardize public image paths, fix strict lint errors.

RecentChanges:

- Validation output analyzed; seed-runner identified at
  src/database/seed/seed-runner-v4enhanced.ts

NextSteps:

- Create seed helper placeholders
- Run pnpm type-check and lint:strict

Blockers:

- .env values not available here (ensure local .env before DB runs)
